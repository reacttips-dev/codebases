import * as React from "react";
import * as flyoutMenuStyles from "../../FlyoutMenu/styles.css";
import AuthenticatedUnknownSvg from "../../SignInOutNav/SignInNav/icon/AuthenticatedUnknownSvg";
import ProfileInitial from "../ProfileInitial";
import * as accountMenuStyles from "../style.css";
import { en, fr } from "./messages";
import * as styles from "./styles.css";
export const GreetingMenu = (props) => {
    const { locale, firstName, profileInitialClassName } = props;
    const messages = locale.startsWith("fr") ? fr : en;
    const greeting = firstName ?
        `${messages.greetingMessage} ${firstName}!` :
        `${messages.greetingUnknownFirstNameMessage}`;
    return (React.createElement("div", { className: styles.greetingContainer, "data-automation": "greeting-container" },
        firstName ?
            React.createElement(ProfileInitial, { firstName: firstName, className: profileInitialClassName }) :
            React.createElement("div", { className: `${accountMenuStyles.authenticatedUnknown} ${flyoutMenuStyles.authenticatedUnknown}`, "data-automation": "authenticated-unknown" },
                React.createElement(AuthenticatedUnknownSvg, { className: "authenticatedUnknown" })),
        React.createElement("span", { className: `${styles.greetingMessage} ${accountMenuStyles.greetingMessage} ${flyoutMenuStyles.greetingMessage}`, "data-automation": "greeting-message" }, greeting)));
};
export default GreetingMenu;
//# sourceMappingURL=index.js.map