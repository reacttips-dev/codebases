import * as React from "react";
import { ChevronRight } from "@bbyca/bbyca-components";
import { cieUtilities } from "../..";
import { SignInNav } from "../SignInOutNav/SignInNav";
import GreetingMenu from "./GreetingMenu";
import * as styles from "./style.css";
const AccountMenu = ({ cieServiceUrl, locale, firstName, redirectUrl, onClickWhenSignedIn, }) => {
    const handleToggleClick = React.useCallback((e) => {
        e.preventDefault();
        if (onClickWhenSignedIn) {
            onClickWhenSignedIn();
        }
    }, [onClickWhenSignedIn]);
    return (React.createElement("div", { className: styles.accountMenu, "data-automation": "account-menu" }, !!cieServiceUrl && cieUtilities.isUserSignedIn() ?
        (React.createElement("a", { className: styles.accountMenuLink, onClick: handleToggleClick, "data-automation": "greeting-link" },
            React.createElement(GreetingMenu, { locale: locale, firstName: firstName, profileInitialClassName: styles.accountContainerProfile }),
            React.createElement(ChevronRight, { color: "white", className: styles.accountMenuChevron }))) :
        (React.createElement(SignInNav, { locale: locale, cieServiceUrl: cieServiceUrl, redirectUrl: redirectUrl }))));
};
export default AccountMenu;
//# sourceMappingURL=index.js.map