import {defineMessages} from "react-intl";

export default defineMessages({
    days: {id: "countdown.days"},
    hours: {id: "countdown.hours"},
    minutes: {id: "countdown.minutes"},
    seconds: {id: "countdown.seconds"},
});
