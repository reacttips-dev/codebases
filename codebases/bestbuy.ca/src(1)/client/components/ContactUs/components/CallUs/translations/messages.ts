import { defineMessages } from "react-intl";

const messages = defineMessages({
    callUsMessage: {id: "components.contactUs.callUs.call"},
    callUsAvailability: {id: "components.contactUs.callUs.availability"},
});

export default messages;
