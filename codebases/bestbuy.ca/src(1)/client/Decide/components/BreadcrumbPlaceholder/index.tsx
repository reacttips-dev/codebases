import * as React from "react";

import * as styles from "./style.css";

export default (props) => {
    return (
        <div className={styles.breadcrumbPlaceholderContainer} >
            <div className={styles.breadcrumbPlaceholder} />
        </div>
    );
};
