import {defineMessages} from "react-intl";

const path = "productReviewsPage.components.verifiedPurchaserToggle.";

export default defineMessages({
    verifiedPurchaserTitle: {id: `${path}title`, defaultMessage: "Verified Buyers Only"},
});
