import { defineMessages } from "react-intl";

export default defineMessages({
    backBtn: { id: "checkout.containers.manufacturer.backBtn" },
    title: { id: "checkout.containers.manufacturer.title" },
});
