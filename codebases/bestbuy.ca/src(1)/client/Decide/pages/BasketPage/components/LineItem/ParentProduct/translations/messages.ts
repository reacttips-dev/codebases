import {defineMessages} from "react-intl";

export default defineMessages({
    removeItem: {
        defaultMessage: "Remove",
        id: "BasketPage.components.LineItem.ParentProduct.removeItem",
    },
});
