import { defineMessages } from "react-intl";

export default defineMessages({
    backBtn: { id: "checkout.containers.benefits.backBtn" },
    title: { id: "checkout.containers.benefits.title" },
});
