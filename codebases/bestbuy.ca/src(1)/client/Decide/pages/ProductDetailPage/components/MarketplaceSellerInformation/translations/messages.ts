import { defineMessages } from "react-intl";

export default defineMessages({
    sellerRating: { id: "product.containers.pdpPage.sellerRating" },
    soldAndShippedBy: { id: "browse.components.marketplaceSeller.soldAndShippedBy" },
});
