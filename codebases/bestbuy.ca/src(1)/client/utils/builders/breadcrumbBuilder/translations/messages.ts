import {defineMessages} from "react-intl";

export default defineMessages({
    home: {id: "utils.breadcrumbBuilder.home"},
    searchResults: {id: "utils.breadcrumbBuilder.searchResults"},
});
