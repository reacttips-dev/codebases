import styled from 'styled-components'

import { Box } from '../Grid'

const Hint = styled(Box)``
Hint.defaultProps = {
  color: 'grey400',
  fontSize: 2,
  fontWeight: 'normal'
}

export default Hint
