import React from 'react';
import { createAvailableProductsProvider } from './default-available-products-provider';

var _ref = createAvailableProductsProvider(),
    fetchAvailableProducts = _ref.fetchMethod,
    DefaultDataProviderComponent = _ref.ProviderComponent;

export var AvailableProductsProvider = function AvailableProductsProvider(_ref2) {
  var children = _ref2.children,
      availableProductsDataProvider = _ref2.availableProductsDataProvider;
  var CustomDataProviderComponent = availableProductsDataProvider && availableProductsDataProvider.ProviderComponent;
  var DataProviderComponent = CustomDataProviderComponent || DefaultDataProviderComponent;
  return /*#__PURE__*/React.createElement(DataProviderComponent, null, children);
};
export var prefetchAvailableProducts = function prefetchAvailableProducts(customProvider) {
  if (customProvider) {
    customProvider.fetchMethod({});
    return;
  }

  fetchAvailableProducts({});
};
export var resetAvailableProducts = function resetAvailableProducts(customProvider) {
  if (customProvider) {
    customProvider.fetchMethod.reset();
    return;
  }

  fetchAvailableProducts.reset();
};