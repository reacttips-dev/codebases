var INFINITY = '+Inf';
var RenderTrackerBuckets = ['4000', '2000', '1000', '500'];
export var getRenderBucket = function getRenderBucket(renderTime) {
  return renderTime >= 0 ? RenderTrackerBuckets.reduce(function (lowestBucket, currentBucket) {
    return renderTime <= parseInt(currentBucket) ? currentBucket : lowestBucket;
  }, INFINITY) : null;
};