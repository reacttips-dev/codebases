export var cardAction = function cardAction(type, _ref, payload, error) {
  var url = _ref.url,
      hasExpired = _ref.hasExpired;
  return {
    type: type,
    url: url,
    hasExpired: hasExpired,
    payload: payload,
    error: error
  };
};