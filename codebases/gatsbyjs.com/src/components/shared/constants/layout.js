const MainContentWidth = {
  default: `54rem`,
  withSidebar: `42rem`,
}

const TocWidth = `18rem`

const ShowcaseSidebarMaxWidth = `15rem`

const PluginsSidebarWidth = {
  default: `21rem`,
  large: `24rem`,
}
const BannerHeight = "3rem" // 36px

const NavHeight = {
  Default: "4rem", // 64px
}

const SidebarItemMinHeight = "2.5rem"

const SidebarUtilityHeight = "3.5rem"

const SidebarWidth = {
  default: `16.5rem`,
  large: `18rem`,
  mobile: `320px`,
}

const HeaderPaddingTopDesktop = `3rem`

const LayoutWidth = 80

const HeaderHeight = `${parseFloat(BannerHeight) +
  parseFloat(NavHeight.Default)}rem`

export {
  MainContentWidth,
  TocWidth,
  ShowcaseSidebarMaxWidth,
  PluginsSidebarWidth,
  BannerHeight,
  NavHeight,
  HeaderHeight,
  SidebarItemMinHeight,
  SidebarWidth,
  HeaderPaddingTopDesktop,
  LayoutWidth,
  SidebarUtilityHeight,
}
