import keysToConstants from 'js/lib/keysToConstants';

const exported = keysToConstants(['VerifiedCertificate', 'Specialization']);

export const { VerifiedCertificate, Specialization } = exported;

export default exported;
