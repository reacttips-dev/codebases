export const NEXT_APP_NAME = 'next';

// If disabling preact, update this and constants in ./scripts/addDepsForNextDev and static/scripts/prepNextDependencies to false.
// Along with removing preact packages in next/package.json.
export const ENABLE_PREACT = false;
