// This file in static requires a file that's only available in static.crane,
// js/app/config, which Crane generates matching whatever config Crane was run with
import config from 'js/app/config';

/* eslint-disable import/prefer-default-export */
export { config };
