import ImageButton from './ImageButton';
import type { Props as ImageButtonProps } from './ImageButton';

const exported = {
  ImageButton,
};

export default exported;
export { ImageButton };
export type { ImageButtonProps };
