export enum LinkProviderType {
    /* Link provider types compatible with server definition. Please don't change its value. */
    OneDrivePro = 0,
    OneDriveConsumer = 1,
    Dropbox = 2,
    Box = 3,
    GDrive = 5,

    /* Client only link provider types */
    OfficeOneDrivePro = 31,
}
