import { action } from 'satcheljs';

export const initializeOwaObservableDateTime = action('initializeOwaObservableDateTime');

export const observableNowChanged = action('observableNowChanged');
export const observableTodayChanged = action('observableTodayChanged');
