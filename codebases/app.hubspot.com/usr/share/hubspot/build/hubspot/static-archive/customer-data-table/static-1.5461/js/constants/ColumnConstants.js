'use es6'; // This is the smallest a person can make any column when resizing it manually

export var ALL_COLUMNS_MIN_RESIZABLE_WIDTH = 200; // This value is set on the column record, so in theory it's the minimum a specific column can be resized to, but I don't see it working.

export var DEFAULT_COLUMN_MIN_RESIZE_WIDTH = 200;
export var DEFAULT_COLUMN_MIN_WIDTH = 100;
export var DEFAULT_COLUMN_WIDTH = 300;