'use es6';

import { Record } from 'immutable';
var ScrollPercentageTrigger = new Record({
  enabled: false,
  scrollPercentage: 50
}, 'ScrollPercentageTrigger');
export default ScrollPercentageTrigger;