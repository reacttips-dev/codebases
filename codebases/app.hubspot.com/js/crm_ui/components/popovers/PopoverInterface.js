'use es6';

import PropTypes from 'prop-types';
export default {
  setPopoverTarget: PropTypes.func.isRequired,
  setPopoverTargetAsRef: PropTypes.func.isRequired,
  getPopoverTarget: PropTypes.func.isRequired
};