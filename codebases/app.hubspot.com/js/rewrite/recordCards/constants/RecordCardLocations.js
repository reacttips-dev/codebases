'use es6'; // This is a special location on the backend that is configured to
// return only a single card. This card stores the properties that are rendered
// on each card on the board.

export var OBJECT_BOARD = 'OBJECT_BOARD';