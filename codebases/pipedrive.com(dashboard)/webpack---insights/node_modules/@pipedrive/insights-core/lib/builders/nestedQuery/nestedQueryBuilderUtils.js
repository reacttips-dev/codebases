"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getQueryVariables = void 0;
const lodash_1 = require("lodash");
const constants_1 = require("../../constants");
exports.getQueryVariables = ({ shouldQueryList = false, shouldQueryExportList = false, shouldQueryChartData = false, reportType, }) => {
    let variables = {};
    if (shouldQueryList) {
        variables = Object.assign(Object.assign({}, variables), { $from: 'Int!', $size: 'Int!' });
    }
    if (shouldQueryExportList) {
        variables = Object.assign(Object.assign({}, variables), { $format: 'String', $fields: '[String]!' });
    }
    if (shouldQueryChartData && constants_1.deals.REPORTS_REQUIRING_PAGE_AND_SORT_ARGS.includes(reportType)) {
        variables = Object.assign(Object.assign({}, variables), { $lastPage: 'Int', $chartDataSize: 'Int', $segmentSize: 'Int', $sortOrder: 'SortOrder', $sortBy: 'SortBy' });
    }
    return lodash_1.isEmpty(variables) ? null : variables;
};
