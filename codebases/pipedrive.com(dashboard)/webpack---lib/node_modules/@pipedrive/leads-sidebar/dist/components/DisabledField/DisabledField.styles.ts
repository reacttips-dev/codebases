import { Button as CUIButton } from '@pipedrive/convention-ui-react';
import styled from 'styled-components';
import { fonts, colors } from '@pipedrive/convention-ui-css/dist/js/variables';

import { paddingField } from '../SidebarFieldWrapper/SidebarFieldWrapper.styles';

export const Button = styled(CUIButton)`
	visibility: hidden;
	pointer-events: none;
`;

export const Wrapper = styled.div`
	${paddingField}
	box-sizing: border-box;
	display: flex;
	position: relative;
	align-items: center;
	&:hover {
		${Button} {
			visibility: visible;
		}
	}
`;

export const Content = styled.div`
	position: relative;
	flex: 1;
	display: flex;
	align-items: center;
	min-height: 32px;
	hyphens: auto;
	white-space: pre-line;
	margin-right: 4px;
	word-break: break-word;
	a {
		text-decoration: none;
		color: ${colors.blue};
		font-weight: ${fonts.fontWeightBold};
	}
`;

export const ButtonWrapper = styled.div`
	align-self: flex-start;
	padding-top: 2px;
`;
