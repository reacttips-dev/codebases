import styled, { css } from 'styled-components';

export const ComponentReadWrapper = styled.div<{ isMultipleOptions: boolean }>`
	white-space: nowrap;
	overflow: hidden;
	text-overflow: ellipsis;

	${({ isMultipleOptions }) =>
		isMultipleOptions &&
		css`
			& > ul {
				display: inline-flex;
				max-width: 100%;
			}

			li {
				max-width: 100%;
			}

			.cui4-tag {
				display: block;
			}
		`}
`;
