export function formatGraphQLTime(input: string | null): string {
	if (input === null) {
		return '';
	}
	return input.split(':', 2).join(':');
}

export function formatGraphQLTimeInRange(input: string | null): string {
	return formatGraphQLTime(input) || '…';
}
