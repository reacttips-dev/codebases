export function formatGraphQLDate(input: string | null): string {
	if (input === null) {
		return '';
	}
	return new Date(input).toDateString();
}

export function formatGraphQLDateInRange(input: string | null): string {
	return formatGraphQLDate(input) || '…';
}
