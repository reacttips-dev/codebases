import React from 'react';

import * as S from './FieldLabel.styles';

type Props = {
	readonly label: string;
};

export const FieldLabel: React.FC<Props> = ({ label }) => (
	<>
		<S.LabelDot />
		<S.Label>{label}</S.Label>
	</>
);
