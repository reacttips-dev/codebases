import * as React from "react";
import { StyledNoTouchTitle } from "./StyledComponents";

const NoTouchTitle = ({ children }) => <StyledNoTouchTitle>{children}</StyledNoTouchTitle>;

export default NoTouchTitle;
