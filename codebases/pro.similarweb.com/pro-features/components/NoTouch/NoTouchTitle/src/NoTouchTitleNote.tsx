import * as React from "react";
import { StyledNoTouchTitleNote } from "./StyledComponents";

const NoTouchTitleNote = ({ children }) => (
    <StyledNoTouchTitleNote>{children}</StyledNoTouchTitleNote>
);

export default NoTouchTitleNote;
