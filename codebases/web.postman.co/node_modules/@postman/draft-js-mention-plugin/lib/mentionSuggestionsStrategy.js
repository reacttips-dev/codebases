'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _findWithRegex = require('find-with-regex');

var _findWithRegex2 = _interopRequireDefault(_findWithRegex);

var _lodash = require('lodash.escaperegexp');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

//A decorator strategy function for MentionSuggestions portal.
exports.default = function (trigger, regExp) {
  return function (contentBlock, callback) {
    (0, _findWithRegex2.default)(new RegExp('(\\s|^|.)' + (0, _lodash2.default)(trigger) + regExp, 'g'), contentBlock, function (start, end) {

      // For example if {xyz is the input, then regular expression returns range for {xyz. This case is handled in if below.
      if (start === 0 && contentBlock.getText().charAt(0) === trigger) {
        callback(start, end);
      }

      // For example example.com/{xyz is the input, then regular expression returns range for /{xyz. Thus the else part uses start+1 to get only {xyz.
      else {
          callback(start + 1, end);
        }
    });
  };
};