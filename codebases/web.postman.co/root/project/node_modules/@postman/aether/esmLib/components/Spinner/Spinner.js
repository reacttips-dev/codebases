import React from 'react';
import PropTypes from 'prop-types';
import SpinnerSmall from './SpinnerSmall';
import SpinnerLarge from './SpinnerLarge'; // following this approach since we are going to add more sizes in the future

var SPINNER_SIZE_CONSTANTS = ['small', 'large'];
export default function Spinner(props) {
  var size = props.size,
      className = props.className,
      description = props.description;

  if (size === 'small') {
    return /*#__PURE__*/React.createElement(SpinnerSmall, {
      className: className,
      description: description
    });
  }

  if (size === 'large') {
    return /*#__PURE__*/React.createElement(SpinnerLarge, {
      description: description || 'Loading',
      className: className
    });
  }

  return null;
}
Spinner.defaultProps = {
  size: 'small',
  className: '',
  description: ''
};
Spinner.propTypes = {
  size: PropTypes.oneOf(SPINNER_SIZE_CONSTANTS),
  className: PropTypes.string,
  description: function description(_ref) {
    var _description = _ref.description;

    if (typeof _description !== 'string') {
      return new Error('Spinner: description prop should have a string value');
    }

    if (_description.length > 80) {
      return new Error('Spinner: the description prop should not have a character length of more than 80.');
    }

    return null;
  }
};