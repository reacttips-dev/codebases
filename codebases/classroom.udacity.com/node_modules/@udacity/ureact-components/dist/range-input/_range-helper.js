"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.determineBenchmarks = determineBenchmarks;

function determineBenchmarks(min, max, step) {
    /* determines where the benchmarks should be placed in the range
     * track, as a percentage of overall width, and associated with
     * the range value
     */

    var stepPercentage = 100 * step / (max - min);

    var benchmarks = [];
    var count = 1;
    for (var percent = stepPercentage; percent < 100; percent += stepPercentage) {
        benchmarks.push({
            percent: percent,
            value: min + count * step
        });
        count++;
    }
    return benchmarks;
}