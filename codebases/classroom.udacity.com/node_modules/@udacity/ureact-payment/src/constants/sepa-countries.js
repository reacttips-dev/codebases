const SEPA_COUNTRIES = ['DE', 'AT', 'CH'];

function isCountrySepaEnabled(country) {
    return SEPA_COUNTRIES.indexOf(country) !== -1;
}

export default {
    SEPA_COUNTRIES,
    isCountrySepaEnabled
}