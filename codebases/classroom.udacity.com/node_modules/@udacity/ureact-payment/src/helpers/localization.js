import * as UreactI18n from '@udacity/ureact-i18n';
import * as moment from 'moment';

const locales = {
    'de': require('../locales/de'),
    'pt-br': require('../locales/pt-br')
};

const options = {
    // debug mode allows for helpful development features like pseudolocalization
    // debug: true,
    moment
};

export const i18n = new UreactI18n.I18n(locales, options);
export const __ = i18n.__;