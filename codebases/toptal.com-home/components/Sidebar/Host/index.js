import React from 'react'

import {
    HOST_ELEMENT_ID
} from '../lib/constants'

/**
 * Host element which will contain all rendered components
 */
const Host = props => < div { ...props
}
id = {
    HOST_ELEMENT_ID
}
/>

export default Host