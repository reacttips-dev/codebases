import { createConfirmation } from 'react-confirm';
import ConfirmDialog from './ConfirmDialog';
export const confirm = (props) => createConfirmation(ConfirmDialog)(props);
//# sourceMappingURL=index.js.map